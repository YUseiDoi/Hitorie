<div class="under-header-container"  data-animsition-in-class="fade-in">
<div class="under-header3">
<%# app/assets/images %>
<%= image_tag "icon.png", { alt: "Rails icon", id: "assets", class: "image", width: "35px", height: "35px" } %>
</div>
<div class="under-header">
<p>ヒトリエ</p>
</div>
<div class="under-header2">
<div class="under-header-section">
<%= link_to "about", shops_about_path %>
</div>
<div class="under-header-section">
<%= link_to "mypage", user_path(current_user.id) %>
</div>
<div class="under-header-section">
<%= link_to 'log out', destroy_user_session_path, method: :delete %>
</div>
<div class="under-header-section">
<%= link_to "top", top2_path %>
</div>
</div>
</div>

<div class="edituser-title">
<h2>プロフィール編集</h2>
</div>

<div class="edituser-box">

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "users/shared/error_messages", resource: resource %>

  <div class="username">
  <div class="field">
    <%= f.label :username, "ユーザーマイページ" %><br />
    <%= f.text_field :username, autofocus: true, autocomplete: "name" %>
  </div>
  </div>

  <!--アカウント編集時にプロフィールを入力できるようにフォームを追加-->
  <div class="profile-container">
  <div class="field">
    <%= f.label :profile, "プロフィール" %><br />
    <%= f.text_area :profile, autofocus: true, size:"45×20" %>
  </div>
  </div>

  <div class="profile-image">
  <div class="field">
  <h2>
  <%= f.label :image, "プロフィール画像" %><br />
  </h2>
    <p><%= f.file_field :image %></p>
  </div>
  </div>

  <div class="email3">
  <div class="field">
    <%= f.label :email, "メールアドレス" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="email">
  <div class="field">
    <%= f.label :password, "パスワード" %> <i>（変更しない場合には空欄）</i><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
    <% if @minimum_password_length %>
      <br />
      <em>6文字以上</em>
    <% end %>
  </div>
  </div>

  <div class="email">
  <div class="field">
    <%= f.label :password_confirmation, "パスワードの確認" %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>
  </div>

  <div class="email">
  <div class="field">
    <%= f.label :current_password, "現在のパスワード" %> <i>（プロフィールの更新には現在のパスワードが必要です）</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password" %>
  </div>
  </div>

  <div class="email">
  <div class="actions">
    <%= f.submit "更新する" %>
  </div>
  </div>
<% end %>

<div class="email2">
<h3>アカウントを削除する</h3>

<p><%= button_to "アカウントの削除", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

</div>

</div>

<div class="above-footer4">
</div>
